<?php
/**
 * 設定ファイル
 *
 * このファイルを config.php にコピーして、実際の値を書き込んでください。
 * config.php は .gitignore に入っているので、GitHubには上がりません。
 */
return [

    // ---- カゴヤDB接続 ------------------------------------------------
    'db' => [
        'host' => 'mysql57s-XX.kagoya.net',   // phpMyAdminのホスト名と同じもの
        'name' => 'lerisa_xxxxx',
        'user' => 'lerisa_xxxxx',
        'pass' => '',
    ],

    // ---- 画面表示 ----------------------------------------------------
    'site_name' => '運営管理',
    'site_sub'  => 'Owner Console',   // サイドバーのロゴ下に出る小さな文字

    // users.status の意味づけ。契約中を表す値を指定します。
    'status_active_value' => 1,
    'status_labels' => [
        1 => '契約中',
        0 => '停止',
    ],

    // users.ktype（プラン）の表示名。実際のプラン名に書き換えてください。
    'plan_labels' => [
        0 => 'プラン0',
        1 => 'プラン1',
        2 => 'プラン2',
        3 => 'プラン3',
        4 => 'プラン4',
    ],

    // kiten_girl.kitengirl_status の表示名
    'girl_status_labels' => [
        0 => '停止',
        1 => '稼働中',
        2 => 'その他',
    ],

    // exeserver に入りうる正常な値。これ以外は整合性チェックで警告します。
    'valid_exeservers' => [0, 1, 2, 3],

    // 最終成功からこの時間を過ぎた契約中店舗を「停止中の疑い」として警告します。
    'stale_hours' => 6,

    // 一覧の1ページあたり件数
    'per_page' => 50,

    // ---- iMacros のログ ----------------------------------------------
    // 店舗ごとのログの置き場所。この下に {folder_name}/ が並びます。
    'log_root' => '/home/lerisa/public_html/kiten',

    // report の行で「残数 > 0 なのに走査数がこれ未満」ならリストが読めていないと見なします。
    'log_scan_min' => 5,

    // ログ明細の1ページあたり行数（ミテネは1日1,300行を超えることがあります）
    'log_per_page' => 200,

    // ---- 編集の許可 --------------------------------------------------
    // false にすると、全画面が閲覧専用になります。
    // 稼働中システムの設定テーブルを直接触るので、慣れるまでは false 推奨です。
    'allow_edit' => false,
];
